apiVersion: v1
kind: Service
metadata:
  name: wso2apim-with-analytics-rdbms-service
  namespace: wso2
spec:
  type: ClusterIP
  selector:
    deployment: wso2apim-with-analytics-mysql
  ports:
    - name: mysql-port
      port: 3306
      targetPort: 3306
      protocol: TCP
